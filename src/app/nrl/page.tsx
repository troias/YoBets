import { TerminalPage } from "@/features/dashboard/components/terminal-page";

export default function NRLPage() {
  return (
    <TerminalPage
      title="NRL Intelligence Terminal"
      subtitle="Head to Head, line, totals, try scorers, steam moves, CLV, and Bet365 Hunter Mode"
      activePath="/nrl"
    />
  );
}
